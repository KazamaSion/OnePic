Vue.use(window['vue-validator']);

new Vue({
  data: {
    name: 'a',
    range: 1
  },
  ready: function() {
    setTimeout(function(){
      this.$emit('name-loaded', this.namee);
      this.$emit('range-loaded', this.range);
    }.bind(this), 500);
  }
}).$mount("#app");
